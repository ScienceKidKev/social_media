<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width-device-width, initial-scale=1">
        <title> Hi</title>

    
    </head>
    <body>
        <h1>Post Feed</h1>
        {% for post in posts %}
            <div>
                <h1>{{post['timestamp'].strftime('%b, %-d, %Y, %I:%M')}}</h1>
                <h2>{{post['display_name']}}({{post['username']}})</h2>
                <img src="{{post['post_image']}}" width="200">
                <p>{{post['post_text']}}</p>
                
            </div>
        {% endfor %} 
    </body>


</html>